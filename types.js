
export const AppStep = {
  CREATE_PHOTO: 1,
  CREATE_VIDEO: 2,
};

// The UploadedFile interface was removed as it's only used for TypeScript type-checking.
// The code will still function correctly without it in a JavaScript environment.
